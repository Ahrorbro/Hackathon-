# Wait App — Admin dashboard (web)

**Purpose:** Review venue claims (approve/reject within 48h per SoW). For Lester / admins.

**Planned:** Next.js or Vite + React; same Supabase project; admin-only auth (e.g. email allowlist or `role = 'admin'` in DB). Screens: Login → Pending claims list → Approve/Reject per claim.

**When:** After or in parallel with Venue Management (Phase 4). Build plan: `../docs/Wait-App-Build-Plan.md`.

**Status:** Not started. Mobile app (Foundation) is in progress first.
